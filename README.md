# The-Litrary-Lounge
I built this project using a modern full-stack architecture. The frontend is built with vanilla HTML, CSS, and JavaScript using a Glassmorphic design. The main backend is built on Node.js and Express, connected to a local SQLite database. For the AI features, I created a separate microservice using Python and Flask.
